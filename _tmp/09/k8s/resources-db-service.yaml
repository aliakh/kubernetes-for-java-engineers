apiVersion: v1
kind: Service
metadata:
  name: resources-db-service
  namespace: microservices
spec:
  selector:
    app: resources-db
  type: ClusterIP
  ports:
    - port: 5432
      targetPort: 5432
