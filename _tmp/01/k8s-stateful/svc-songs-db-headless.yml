apiVersion: v1
kind: Service
metadata:
  name: songs-db
  namespace: k8s-program
spec:
  clusterIP: None
  selector:
    app: songs-db
  ports:
    - port: 5432
      targetPort: 5432
      name: postgresql
