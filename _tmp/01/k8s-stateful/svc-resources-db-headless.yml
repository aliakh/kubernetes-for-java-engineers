apiVersion: v1
kind: Service
metadata:
  name: resources-db
  namespace: k8s-program
  labels:
    app: resources-db
spec:
  clusterIP: None
  selector:
    app: resources-db
  ports:
    - port: 5432
      targetPort: 5432
      name: postgresql
