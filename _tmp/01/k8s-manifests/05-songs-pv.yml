apiVersion: v1
kind: PersistentVolume
metadata:
  name: songs-pv
  namespace: k8s-program
spec:
  storageClassName: manual
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  hostPath:
    path: "/mnt/data/songs-db"
