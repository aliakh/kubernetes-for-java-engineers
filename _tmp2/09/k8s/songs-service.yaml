apiVersion: v1
kind: Service
metadata:
  name: songs-service
  namespace: microservices
spec:
  selector:
    app: songs
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8081
      nodePort: 30001
  type: NodePort
